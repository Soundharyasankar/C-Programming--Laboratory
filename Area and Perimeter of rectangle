#include<stdio.h>
int main()
{
    int l,b;
    scanf("%d%d",&l,&b);
    int area;
    area=l*b;
    int perimeter;
    perimeter=2*(l+b);
    printf("Area of Rectangle=%d\n",area);
    printf("Perimeter of Rectangle=%d",perimeter);
return 0;
}
